interface:
  display_name: "KENUXA OPS Release CI"
  short_description: "Verify and debug KENUXA OPS release validation runs"
  default_prompt: "Use $release-KENUXA OPS-ci to preflight provider secrets, watch full release validation, summarize child runs, and triage only failing release lanes."
