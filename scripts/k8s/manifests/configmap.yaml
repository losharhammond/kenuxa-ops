apiVersion: v1
kind: ConfigMap
metadata:
  name: KENUXA OPS-config
  labels:
    app: KENUXA OPS
data:
  KENUXA OPS.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.KENUXA OPS/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "KENUXA OPS Assistant",
            "workspace": "~/.KENUXA OPS/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # KENUXA OPS Assistant

    You are a helpful AI assistant running in Kubernetes.
